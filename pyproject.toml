[project]
name = "music-streaming-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vishal560", email = "vishal@560mtek.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
music-streaming-db = "music_streaming_db:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
